<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pushpushgo.pushpushgo_sdk_example">
       
    <!-- Moved from ${applicationName} -->

    <application
        android:label="pushpushgo_sdk_example"
        android:name=".MainApplication" 
        android:icon="@mipmap/ic_launcher">
        
        <!-- PushPushGo SDK Configuration (prevents cold-start crash) -->
        <!-- Uncomment and fill with your credentials:
        <meta-data android:name="com.pushpushgo.sdk.projectId" android:value="YOUR_PROJECT_ID" />
        <meta-data android:name="com.pushpushgo.sdk.apiKey" android:value="YOUR_API_KEY" />
        <meta-data android:name="com.pushpushgo.sdk.isProduction" android:value="true" />
        -->
        
        <!-- In-App Messages Configuration (early initialization via ContentProvider) -->
        <!-- Uncomment and fill with your credentials:
        <meta-data android:name="com.pushpushgo.inapp.projectId" android:value="YOUR_PROJECT_ID" />
        <meta-data android:name="com.pushpushgo.inapp.apiKey" android:value="YOUR_API_KEY" />
        <meta-data android:name="com.pushpushgo.inapp.isDebug" android:value="false" />
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />

            <intent-filter>
                <action android:name="APP_PUSH_CLICK" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
